<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.iteso.vizkar_v1">

    <!-- To access Google+ APIs: -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Network location : -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- GPS location -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/penguin_icono"
        android:label="@string/app_name"
        android:roundIcon="@drawable/penguin_icono"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ActivitySplashScreen"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activityMain"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activityLoginScreen"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activitySignUpScreen"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ActivityInicial"
            android:label="@string/title_activity_inicial"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ActivityViewImage"
            android:label="@string/title_activity_view_image"></activity>
    </application>

</manifest>